[package]
name = "shared_infrastructure"
version = "0.1.0"
edition = "2024"
description = "Shared infrastructure layer for the Crust project"
license = "MIT OR Apache-2.0"

[dev-dependencies]
tempfile = "3.8"