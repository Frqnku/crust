[package]
name = "crust_scaffolding_domain"
version = "0.1.0"
edition = "2024"
description = "Scaffolding domain layer for the Crust project"
license = "MIT OR Apache-2.0"

[dependencies]
crust_shared_domain = { path = "../../shared/domain", version = "0.1.0" }