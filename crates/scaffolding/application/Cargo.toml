[package]
name = "crust_scaffolding_application"
version = "0.1.0"
edition = "2024"
description = "Scaffolding application layer for the Crust project"
license = "MIT OR Apache-2.0"

[dependencies]
crust_scaffolding_domain = { path = "../domain", version = "0.1.0" }
crust_shared_domain = { path = "../../shared/domain", version = "0.1.0" }

[dev-dependencies]
tempfile = "3.8"