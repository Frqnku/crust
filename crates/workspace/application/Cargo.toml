[package]
name = "workspace_application"
version = "0.1.0"
edition = "2024"
description = "Workspace application layer for the Crust project"
license = "MIT OR Apache-2.0"

[dependencies]
workspace_domain = { path = "../domain", version = "0.1.0" }
shared_domain = { path = "../../shared/domain", version = "0.1.0" }